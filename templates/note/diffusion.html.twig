
<div class="modal fade" id="diffusion" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <center><h5 class="modal-title text-center">SELECTIONNER LES DESTINNATAIRES</h5></center>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body">
                {{ form_start(form) }}
                {{ form_label(form.destinataires) }} <br>
                {{ form_widget(form.destinataires) }}
                
            </div>
            <div class="modal-footer">
                {{ form_row(form.diffuser, {'attr': {'class': 'btn-success mb-2'}}) }}
                {{ form_end(form) }}
                <button type="button" class="btn btn-danger" data-dismiss="modal">Annuler</button>
            </div>
        </div>
    </div>
  </div>

{# {% extends 'form.html.twig' %}

{% block content %}
    <h1>Diffusion de la Note</h1>

    <h2>Note : {{ note.objet }}</h2>

    <form method="post">
        {{ form_row(form.destinataires) }}

        <button type="submit" class="btn btn-primary">Diffuser</button>
    </form>
{% endblock %} #}
  







